# ShadowLink Protocol — Reference

Steganographic VPN. Go 1.25, `module github.com/nixavpn/shadowlink`. Отдельный
проект (`D:\shadowlink`) со своим go.mod и git — **вне дерева NixaVPN**.
Противник — **TSPU** (DPI РКН); из этого растут все архитектурные решения.

## Hard rules

1. **НЕ предлагать CDN / Cloudflare / domain fronting.** Прод = DIRECT к голому
   origin IP. ⚠ Обоснование уточнено 2026-08-07: прежнее «РКН режет диапазоны CDN
   целиком» — форумное, peer-reviewed подтверждения нет. Более сильный довод —
   ACM IMC 2022: ТСПУ стоит **inline на краю каждого ISP**, а не на IX, поэтому
   природа origin не меняет того, что решение принимается на первом хопе от
   абонента. Domain fronting отдельно мёртв с 2018 — крупные CDN его отключили.
2. **НЕ рандомизировать TLS fingerprint.** Только Chrome. Рандомный FP уникален,
   а уникальность и есть сигнал — это довод самодостаточный, замера не требует.
   ⚠ Тезис «Firefox/Safari РКН банит первыми» — форумный и **оспорен**
   (Xray-core#6293, 2026-06: обратная рекомендация); не считать доказанным.
   ⚠ Chrome 133 — это **потолок uTLS**, а не выбор: профилей выше в библиотеке
   нет. Значит отпечаток неизбежно дрейфует относительно живого Chrome. Починить
   выбором версии нельзя; ранний сигнал — доля Chrome 133 в реальном трафике.
3. **Горячий путь = WebSocket binary frames.** JSON-envelope только на handshake
   POST + cover.
4. **nginx обязателен** — Go `net/http` шлёт non-browser HTTP/2 SETTINGS (JA3-риск).
5. **Серверный бинарь — единственная копия** `bin/shadowlink-server-linux` здесь.
   Вторая копия однажды дала silent «binary identical, skipping upload».
6. **Windows-dev: `-race` недоступен** (нет gcc). Зелёный локальный прогон ≠ нет гонки.
7. **Коммиты и push делает пользователь.** Не коммитить без явной просьбы.
8. Тайминговые константы — не менять «на глаз». ⚠ Окно ~130–190 с **НЕ
   подтверждено** и, по замеру 2026-08-07, **неверно**: наблюдаемое окно на
   рабочем AS — **84–118 с** (p10 84.1 / p50 97.6 / p90 118.0, n=222). Ось реза —
   **возраст**: CV 0.26 против 8.73 по байтам (в 33 раза плотнее); умирают и
   слоты с 0 КБ, поэтому триггер по объёму из net4people#490 на этом AS **не
   воспроизводится**. ⚠ И **глобальная константа неверна в принципе**:
   ACM IMC 2022 — ТСПУ неоднородна по регионам и операторам. Курс — адаптация
   per-AS на клиенте (`client/slotobs`).
   **Считать бюджет только через `WSPoolTransport.worstCaseTeardown()`.** Он
   складывает `base + stagger + sweep + deferred + teardown_cap`, где `sweep` с
   2026-08-14 = тик + подтиковое размазывание фазы. До 2026-08-07 лог считал
   `base + sticky` и врал в 1.6 раза: выпадал `staggerOffsetCap` (до +48 с), из-за
   чего 8 ячеек из 16 имели порог ротации 114 с при p10 смертей 84 с и физически
   не доживали до собственной ротации. Никогда не складывать слагаемые по месту.
   ⚠ Выборка `slotobs` **цензурирована**: `Record` вызывается только на ошибке
   чтения (`ws_pool.go`), плановые ротации в неё не попадают. Поэтому доля
   «срезано посредником» из `by_close_kind` не выводится, а p10 смещён вверх.
9. **Возрасты смертей описывают НАШ порог, а не цензора** (аудит 2026-08-13,
   лог `nixavpn-DEBUG-20260813-083912`). 9 резов легли в 82.6–84.0 с (ширина
   1.4 с, CV 0.005) — соблазн прочесть это как «окно сжалось». Восстановление
   времён жизни 870 плановых ротаций опровергло: **337 слотов прожили дольше
   самого позднего реза и были сняты нами штатно**, до 90 с дожили 12, max
   104.7 с. Стены на 84 с нет; узость полосы = узость наших порогов (79.7 и
   85.2 с). Смерти детерминированы stagger: slot 0–5 (79.7 с) — **0** смертей
   на 613 ротаций, slot 6–9 (85.2 с) — **9** на 244.
   → Перед любым выводом об окне восстанавливать времена жизни **плановых**
   ротаций (`reader started` → `teardown`), а не только резы. Предупреждение
   уже стоит в `client/stats.go:1446` и `client/slotobs/planned.go:19`.
   ⚠ `cause=age_cut` — **ярлык, не наблюдение**: `isAgeCut()`
   (`client/ws_pool.go:1313`) метит любую терминальную ошибку старше
   `ageCutMinAge`, тип ошибки не смотрится. «Все смерти — age_cut» тавтологично.
   ⚠ «Ladder stagger дремлет, реальные offsets ~4.7 и ~10.2 с» — **причина
   названа неверно** (замер 2026-08-14). Лестница не дремлет: offsets реально
   равны `idx ± 0.5 с`, из 16 значений используется 9–12. Два режима в логе —
   следствие **квантования `rotationWatchdogTick = 5 с`**: порог проверяется
   только на тике, поэтому 4.7 и 9.7 — это не offsets, а порог, округлённый
   вверх до сетки тика; offsets 0…4 с неразличимы, 5…9 с неразличимы. Модель
   подтверждена на двух прогонах (base 70 → 73.8/79.6 с; base 75 → 79.7/84.7 с).
   Вывод «8 ячеек из 16 с порогом 114 с» (п. 8) всё равно не воспроизводится.
   ⚠ **Фаза свипа уходила на wire, и лечится она НЕ величиной периода, а его
   непостоянством.** Замер 08-13: все 588 age-ротаций в одной фазе 5-секундной
   сетки (σ 0.0001 с), 92.2 % TCP-connect'ов в одном односекундном окне.
   Комментарий про размазанный FFT-пик описывал механизм, который **не
   исполнялся**: stagger-джиттер прибавляется к порогу, а порог проверяется лишь
   на тике. Две попытки и их итог:
   1. ручка «джиттер на момент дренажа» через `nextDrainAttemptNs` — **откачена**:
      её читает только свип, который сам просыпается по тому же тикеру, поэтому
      отсрочка короче тика переносила дренаж ровно на следующий тик, в ту же фазу;
   2. срезка `rotationWatchdogTick` 5 с → 500 мс — **помогла частично**: линия на
      0.2 Гц убита (vector strength 1.0000 → 0.0096), но пик **переехал на 2 Гц**
      (R на периоде 500 мс = 0.8657). Уменьшение периода воспроизводило бы тот же
      результат на следующем масштабе;
   3. **`time.Timer` с перевзводом на `tick + uniform[0, tick)`**
      (`nextWatchdogWakeup()`) — решётки не остаётся **у этого цикла**. Джиттер
      сэмплируется заново каждый взвод (один сэмпл на сеанс = решётка с другим
      шагом).
   ✅ **ЗАМЕРЕНО 2026-08-14 (прогон 161915, 4 ч): фаза развязана ЦЕЛИКОМ**, включая
   момент закрытия — прогноз «развязано наполовину» оказался пессимистичным.
   `R` по всем событиям и периодам на уровне шума (`1/√n` = 0.025):

   | Событие @ 0.5 с | база (тик 5 с) | 500 мс без джиттера | **Timer+джиттер** |
   |---|---|---|---|
   | `reader started` | 0.5892 | 0.6023 | **0.0330** |
   | `drain started` | 0.9541 | 0.8900 | **0.0464** |
   | close (`natural finish`) | 0.9655 | 0.8933 | **0.0447** |

   Слепой скан 0.05–6.0 с шагом 5 мс: max `R` = 0.063 — линий нет во всём диапазоне.
   **Механизм, из-за которого close тоже развязался: ЯКОРЬ.** Сетка дренажа жива
   на 100% — `R((close − drain_start) mod 500мс) = 1.0000`, **1575/1575** событий
   (все терминации, не только natural finish) в одном 50-мс бине, — но её фаза
   наследуется от `startDrain`, а тот приходит из джиттерованного свипа. Правка
   `drainPollInterval` **не нужна**; это зависимость, а не свобода: возврат свипа
   к `time.NewTicker` немедленно проявит решётку 500 мс в моментах close без
   единой правки в коде дренажа.
   ⚠ **Один аргумент за якорь надо предъявлять именно так, иначе он не держит.**
   87 % дренажей (1367 из 1575) закрываются на ПЕРВОМ же тике, то есть
   `close − drain_start` = 0.500 с ровно — константа. По одной константе `R = 1.0`
   получается тривиально и решётку не доказывает. Проверка сделана на подвыборке
   **многотиковых** дренажей (`delta > 0.75 с`, n = 208, до 25.0 с): там тоже
   `R = 1.0000`, а `delta` кратна 500 мс с максимальным остатком 4 мс — вот это и
   есть тикер. Дополнительно: фаза `close` минус фаза `drain_start` (mod 500 мс)
   лежит в 0…5 мс при равномерной абсолютной фазе самого `drain_start`
   (гистограмма по 50-мс бинам: 185/144/187/165/146/159/154/131/141/163) — то
   есть close наследует фазу якоря, а якорь размазан.
   → Сторож, чтобы связь не потерялась при рефакторинге:
   `TestRotationWatchdogLoop_UsesJitteredTimerNotTicker` (читает исходник:
   поведенческий тест на `nextWatchdogWakeup()` остаётся зелёным, даже если цикл
   перестанет её вызывать).
   → Критерий фазы — **`R < 0.2` на периоде тика**, а НЕ «доля в модальном окне
   сетки 5 с»: та падает автоматически при любом тике меньше секунды, то есть
   измеряет нашу ручку, а не наблюдаемость.
   ⚠ Замером НЕ снято: медиана `close − start` = 0.500 с ровно — пара «новое TLS →
   close через полсекунды» жёстко коррелирована по **интервалу**. Это шаблон, а не
   периодичность. И `R` считается по меткам ЛОГА, а не по SYN/FIN на проводе:
   остаточная линия тоньше ~10 мс этим методом не видна.
   ⚠ **Список «прочих незащищённых источников» пересмотрен 2026-08-18 — три
   пункта из четырёх были описаны НЕВЕРНО, а настоящая решётка в нём не
   значилась.** Ревизия: чтение кода + замер по прогону 094311 + независимый
   аудит. Итог по пунктам:
   1. `proxy/socks5/tcp.go:124` и `udp.go:384` (20 мс, «грубейшая решётка 50 Гц»)
      — **недостижимы в прод-конфигурации (SystemVPN/пул), но НЕ мёртвый код**.
      Единственный call-site обоих — ветка `else` диспетчера
      `proxy/socks5/server.go:255/261`, а прод идёт по `WST != nil` (пул).
      Fallback'а по ЗДОРОВЬЮ пула нет: ветка выбирается по полю структуры один
      раз на CONNECT; tun2socks (`inprocess.go:58`) тоже зовёт `tunnelTCPStream`
      напрямую. Замер прогона 094311: `udp_polls` = **0** за 8 ч, `encrypts`
      p50 **6.4/с** при пороге 50/с у одного живого поллера (пол каждого
      интервала был бы 250 на 5 с — наблюдается 0).
      ⚠ **Но «мёртвый код» — формулировка сильнее данных** (ревью 2026-08-18).
      Есть ТРИ конфигурации, где 20-мс циклы живые и это основной режим:
      (а) весь блок создания WS-транспорта под гейтом
      `if e.cfg.SystemVPN || slCfg.WebSocket` (`engine_shadowlink.go:220`) —
      клиент как обычный SOCKS5-прокси без `websocket: true` даёт `WST == nil`;
      (б) стартовый fallback `engine_shadowlink.go:619-628`: при `!poolOK &&
      !SystemVPN` пробуется single WS, и при неудаче — буквально
      `Warn("Single WS не удался, используем poll-mode")`, то есть fallback
      существует, просто на этапе установления, а не при деградации;
      (в) `NIXAVPN_FORCE_PER_STREAM_WS=1` — сервер получает `PerStreamWS` без
      `WST`, а UDP-диспетчер (`server.go:258`) третьей ветки не имеет, поэтому
      **весь UDP идёт через 20-мс тикер**. Цена не нулевая: `PollStreams` =
      `SendStream(ctx, 0, nil)` = реальный шифрованный кадр на каждом тике.
   2. `Connect` fan-out (`idx * staggerDelay`) — **жив, но РАЗОВЫЙ**: 1 исполнение
      на сеанс против 3284 созданий соединений (0.03 %). `staggerDelay` читается
      только в `ws_pool.go:2445-2447`; в reconnect-пути его НЕТ, несмотря на
      комментарий поля `ws_pool.go:1770` («initial/**reconnect** spacing» — врёт).
      Реконнекты разнесены рандомом (`U(0, ageCutReconnectJitter)` / exp-backoff).
      ⚠ Уточнение ревью 2026-08-18, чтобы это не «находили» заново: в
      reconnect-пути есть ДРУГАЯ лестница — `reconnectJitterOffset`
      (`ws_pool.go:968`) даёт `idx × 200 мс ± 100 мс`. Она не детерминированная
      (джиттер ±half-step размазывает фазу внутри шага), но и не случайная:
      математическое ожидание растёт по индексу. И `slotBackoffDuration`
      (`ws_pool.go:1631`) при `attempt ≥ 4` вырождается в **ровно 60.000 с**:
      база 5 с × 2⁴ = 80 с, джиттер ∈ [1.0, 2.0) — кламп 60 с срабатывает
      всегда, поэтому джиттер исчезает целиком. Оба узкие по условиям
      (post-meltdown окно / затяжной отказ), поэтому не дефект, а уточнение.
      ⚠ Ловушка: все 8 строк `slot ready` стоят в окне 8 мс — прочесть это как
      «лестница не сработала» нельзя, строки печатаются в цикле ПОСЛЕ `wg.Wait()`.
      Лестница исполнилась: span fan-out 2.422 с ≈ 7 × 300 мс + handshake.
   3. `connmanager.go:417` (`AfterFunc(2 с)`) — **не решётка**, но исходное
      обоснование было неверным (уточнено ревью 2026-08-18). Интервал ротации
      **log-normal** (μ=ln(300), σ=1.5), а не фиксированные 5–10 мин, и
      `rotate()` зовётся ещё и на любой сетевой ошибке (`connmanager.go:350`).
      Фиксированный лаг 2 с — шаблон пары, не линия.
      ⚠ «У WS-пула своего `ConnManager` нет» — **неверно**:
      `NewWebSocketTransport` (`ws_transport.go:99`) создаёт СВОЙ, по одному на
      коннект слота, и `NewConnManager` при `MinRotation > 0` безусловно
      запускает `startRotation()` (`connmanager.go:124`). Вывод «не решётка»
      устоял по ДРУГОЙ причине: этот per-slot `cm` читается только как
      `ActiveFingerprint()` (`ws_transport.go:200/270/348/845`), запросов не
      делает, поэтому его `CloseIdleConnections()` — no-op на проводе. Верный
      аргумент — «немой», а не «его нет».
      ⚠ И «handshake-путь разовый» неточно: `DirectTransport.cm` несёт кавер и
      хендшейк КАЖДОГО (ре)коннекта слота, так что там `AfterFunc(2 с)` даёт
      настоящие FIN'ы. Якорь пары — момент `rotate()`, а не новый TLS-хендшейк
      (тот случится лениво на следующем `Do`).
   ⚠ **А вот НАСТОЯЩАЯ незащищённая решётка, которой в списке не было:
   `creditSenderInterval = 8 мс` (`client/stream_flow.go:77`) — `time.NewTicker`,
   отправляющий WINDOW_UPDATE, то есть РЕАЛЬНЫЕ байты к origin. 125 Гц.**
   Замерено 2026-08-18 (`TestCreditSender_SendPhaseIsQuantizedByTicker`):
   **R@8мс ≈ 0.95–0.99** при n≈150 и шуме 0.082, интервалы отправок 6.5–9.1 мс.
   Это жёстче, чем было у watchdog до правки (там R 0.59–0.97).
   ⚠ **Записан диапазон, а не точное число, намеренно.** Первая версия правила
   зашила «R = 0.9912» — на другой машине тот же тест дал 0.9465–0.9661
   (10 прогонов), у автора 0.9635–0.9902. Величина зависит от загрузки хоста
   (точность пробуждения тикера), поэтому одна цифра в правиле создала бы
   ложный «регресс 0.99 → 0.95» на следующем аудите. Значим только порядок:
   R ≫ 0.2 — решётка есть.
   ⚠ Возражение «R = 0.99 — артефакт искусственного насыщения теста (64 МБ/с)»
   проверено и **НЕ подтвердилось в части фазы**. Замер на четырёх скоростях:
   0.22 МБ/с (средний в поле) → R = 0.9771 при n=18; 1 МБ/с → 0.9892 (n=75);
   4 МБ/с → 0.9714 (n=195); 20 МБ/с → 0.9574 (n=374). Throughput меняет
   **частоту** отправок, но не их **фазу**: каждая происходит на пробуждении
   тикера и потому лежит на сетке 8 мс при любой скорости. Порог 32 KiB за тик
   (= 4 МБ/с) определяет, слать ли на КАЖДОМ тике, а не попадёт ли отправка на
   сетку.
   ⚠ Но **плотность решётки в поле низкая, и это надо предъявлять вместе с R**:
   прогон 094311 (n = 5750 пятисекундных интервалов, 6.10 ГБ) — средний
   throughput **0.217 МБ/с**, p50 0.001, p90 0.528, p99 **2.601 МБ/с** (то есть
   даже p99 НИЖЕ порога насыщения), интервалов ≥ 4 МБ/с — **12 из 5750 =
   0.21 %**. Оговорка в обе стороны: эти 0.21 % несут **20.1 % трафика**
   (1.22 из 6.10 ГБ) — то есть непрерывная линия 125 Гц держится внутри
   burst'ов большой закачки, ровно когда слот под нагрузкой и живёт долго.
   «Наблюдаемо в 0.21 % времени» ≠ «неважно», но и «настоящая решётка проекта»
   без этой оговорки — сильнее данных. Джиттер в цикле
   ЕСТЬ, но приложен к **порогу** (`thresholdRatio` 0.4–0.6), а не к периоду
   пробуждения — ровно тот дефект, что разобран выше для `rotationWatchdogTick`;
   более того, порог давно не гейтит вовсе (`_ = thresholdRatio`,
   `stream_flow.go:166`), что фиксирует `TestCreditSender_ThresholdRatioDoesNotGateSend`.
   Докстринг `startCreditSender` про «jittered 40-60% threshold» описывает
   механизм, которого нет.
   ✅ **НАБЛЮДАЕМОСТЬ ПОДТВЕРЖДЕНА НА ПРОВОДЕ 2026-08-19** (первый в проекте
   замер по pcap, а не по логу; захват `pktmon` 2 ч 20 мин, 1 603 467 кадров,
   6.5 ГБ). Прежняя оговорка «нужен pcap, из свойства кода наблюдаемость не
   следует» — **снята замером**:
   - uplink-пакеты payload 1…120 Б к origin (n = 71 059): **R@8мс = 0.3114**
     при шуме 0.0038 — в 80 раз выше шума;
   - слепой скан 6…12 мс шагом 0.1 мс: максимум ровно на **0.0080 с**;
   - контрольные периоды 7.3 / 9.1 / 11 / 13 / 17 мс — R = 0.002…0.007 (шум);
   - **линия принадлежит КОНКРЕТНЫМ размерам пакета, а не трафику вообще**
     (разбивка по payload, порог n ≥ 200): 71 Б → **0.9445** (n = 21 991),
     102 Б → 0.3379 (n = 1340), 83 Б → 0.3693, 109 Б → 0.4435, 107 Б → 0.4786,
     1 Б → 0.6494. Итого 6 размеров, **24 593 пакета = 34.6 %** мелких uplink.
     Доминирует 71 Б: 6 Б payload `NewWindowUpdateChunk` (`core/chunk.go:305`)
     + AES-GCM + WS-фрейм + TCP/IP.
   - **Соседние размеры чисты**, и это ключ к тому, что линия не артефакт
     захвата: 75 Б → 0.0185 (n = 18 699), 65 Б → 0.0238 (n = 13 049),
     99 Б → 0.0192, 24/28/80/84/85/86/98/113 Б → 0.010…0.073. Такое расслоение
     нельзя получить дрожанием таймстампов pktmon — оно било бы по всем.
   → Вывод: **это не свойство кода, а наблюдаемая линия 125 Гц у отделимого по
   размеру класса пакетов.** Приоритет правки поднят.
   ⚠ «Ровно один размер (71 Б)» — так было записано в первой версии этой строки
   и это **сильнее данных**: линию дают шесть размеров. Атрибуция каждого к
   своему типу чанка по этому замеру НЕ выводится — размер на проводе зависит от
   payload, паддинга и WS-фрейминга, а сопоставления «размер → Flag» мы не
   строили. Достоверно: линия есть, она у части классов, доминирует 71 Б.
   ⚠ Ротация при этом на проводе ЧИСТА (тот же захват): SYN к origin n=1110
   `R@0.5с = 0.0187`, FIN/RST n=1232 → 0.0599, вместе n=2342 → 0.0332 при шуме
   0.021–0.030; слепой скан 0.05–6.0 с: max 0.101–0.144 — линий нет. Правки
   watchdog подтверждены на пакетах, а не только по меткам slog.
   → **Константу НЕ трогали** (hard rule 8). Лечение по образцу watchdog —
   `time.Timer` с перевзводом на `tick + uniform[0, tick)`; цена правки в том, что
   8 мс выбраны как «cheap tick» под задержку возврата credits, поэтому размазывание
   увеличит p99 задержки credit и может вернуть credit-stall (из-за него слот
   простаивал и его жали с close 1006 — см. комментарий `stream_flow.go:154-160`).
   Перед правкой мерить не только R, но и downlink-throughput.
   ⚠ Второй незакрытый кандидат: `streamAckThrottleInterval = 50 мс`
   (`client/client.go:1201`). Сам по себе НЕ тикер — гейт по метке последней
   отправки, call-site один (`proxy/socks5/tcp.go:1046`, по приходу данных). Но
   гейт «не чаще раза в T от последней отправки» при непрерывном насыщении даёт
   интервалы ровно T, то есть самосинхронизирующуюся линию 20 Гц с фазой от
   начала насыщения. Замером НЕ снято: моменты `SendStreamAck` в лог не пишутся.
   ⚠ Третий: `sendKeepaliveToAllSlots` (`ws_pool.go:2820`) джиттерует МОМЕНТ
   (log-normal, base 5 с), но шлёт всем 8 слотам в одном проходе — на wire это
   синхронный burst 8 фреймов. Разнесения по слотам нет. ✅ Разнесение добавлено
   2026-08-19 (окно 400 мс, вычитается из интервала). ⚠ Полем НЕ подтверждено до
   2026-08-21: строка `keepalive sent` стояла на уровне Trace, а прогоны идут с
   `-log=debug` — 0 строк в логе 08-20. Поднята до DEBUG, и `spread_ms` теперь
   ФАКТИЧЕСКАЯ длительность прохода, а не константа окна (константа
   «подтверждала» бы разнос, повторяя настройку вместо измерения — а
   кумулятивность прохода и была P0 первой версии).
   ✅ **РЕШЁТКА 125 Гц УБИТА, подтверждено НА ПРОВОДЕ 2026-08-20** (pcap
   1 477 513 кадров, 10 ч 36 м, покрывает второй прогон целиком):
   payload 71 Б → `R@8мс = 0.0008` (n=22 272, шум 0.0067) против **0.9445** до
   правки; все мелкие uplink вместе → **0.0018** (n=113 007) против 0.3114;
   остальные пять «грязных» размеров 0.005–0.082. Ack-гейт `R@50мс = 0.0135`.
   Ротация осталась чистой: SYN 0.0138 (n=2804), FIN/RST 0.0228. Слепой скан
   0.05–6.0 с: max 0.0568 — линий нет.
   ✅ **Линия 100 мс ЗАКРЫТА 2026-08-21: она НЕ наша.** На новом захвате (8.04 ч,
   1 487 233 кадра) `R@0.1с` = **0.0064** (uplink, n=705 481, шум 0.0012) против
   0.0390 — прежней линии нет. Остаток сидит **не на 100 мс, а на 1.000 с**, и
   100 мс было его ДЕЛИТЕЛЕМ: uplink 210 Б даёт `R@1.0с = 0.9941` (n=268) при
   `R@0.1с` = 0.9450, `R@0.25с` = 0.9863, `R@0.5с` = 0.9913 — лесенка делителей,
   ровно ловушка слепого скана, описанная ниже в этом же правиле.
   Что это не цикл клиента — доказано ТРЕМЯ независимыми признаками, а не
   предположено: (1) фаза привязана к СТЕННЫМ часам — доля unix-секунды p10
   0.7667 / p50 0.7736 / p90 0.7798, разброс 13 мс за 8 ч; (2) относительно
   собственного потока те же пакеты размазаны — `R(время от SYN) = 0.0245`,
   p50 27.3 с, p90 2377 с; (3) 268 пакетов на **238 разных локальных портов**
   (p50 = 1 пакет на поток), то есть не периодический кадр внутри соединения.
   Интервалы p10 = p50 = 31.008 с при CV 2.85; периода 31 с в клиенте нет.
   Тот же секундный кластер у 141/86/102/106/107/85/98/109 Б (R 0.22–0.50), но
   **фазы разные** — источников много. Совпадает с прикладным трафиком сквозь
   туннель (`dnsproxy arbitrate` R(mod 1с) = 0.2092, n=1813; `msftconnecttest`,
   `chat.google.com`, `linkedin`).
   ⚠ Атрибуция «210 Б = конкретное приложение» НЕ построена — утверждается
   только отрицание. Сопоставление `payload → тип чанка` по-прежнему не сделано.
   ⚠ Решётка 125 Гц на этом захвате тоже чиста (мелкие uplink `R@8мс = 0.0011`,
   n=101 146; 71 Б → 0.0083, n=23 285), но прогон **лёгкий**: интервалов
   ≥ 4 МБ/с всего 2 из 5792 (0.03 % против 0.21 %), поэтому проверка «внутри
   burst'ов» слабее прежней — там n=817…1393 при R 0.02–0.04.

15. **Keepalive обязан доходить до слотов в ДРЕНАЖЕ** (замер 2026-08-20, правка
   2026-08-21). До неё `sendKeepaliveToAllSlots` фильтровал по
   `getState() != slotReady`, и дренируемый слот замолкал ровно в момент
   перехода, а живёт он после этого до `stickyMaxDrainAge` (25 с в поле) при
   пороге реза по тишине 10 с. Итог в поле: **все 5 взрослых резов** (close 1006,
   возрасты 88.4–108.8 с) пришли в слоты, уже бывшие в дренаже, через 8.9–22.3 с
   после `drain started`, и в трёх случаях `last_write_age_ms` = 11.4 / 18.3 /
   **28.2** с. Предикат — `keepaliveWantsState`; сторож —
   `TestKeepalive_ReachesDrainingSlots` (проверяет и обратное: `slotDead` /
   `slotConnecting` кадров НЕ получают).
   ⚠ Дренаж этим не удерживается, и довод не в вероятности: решение «слот пуст»
   принимается по `lastWriteNs` КАЖДОГО СТРИМА (`allStreamsIdle`,
   `stream_entry.go:183`), а keepalive — кадр уровня слота, стримов не касается.
   Транспорт при входе в дренаж жив, закрытие только в `tearDown`.
   ✅ **ПРОВЕРЕНО В ПОЛЕ 2026-08-21** (прогон 102532, 8.05 ч, лог чист целиком —
   ни сна, ни QuickEdit: max gap 5.00 с, 0 инверсий, 5795/5795 тиков stats).
   Разбор — `docs/plans/2026-08-21-field-run-analysis.md`.
   **Правка работает, и гипотеза «рез по тишине» замером ОТВЕРГНУТА.**
   Тишина ушла: `last_write_age_ms` у резов **5.6 / 4.3 / 4.8 с** против
   11.4 / 18.3 / 28.2 с — все ниже порога 10 с. Резы при этом остались (3 за
   8.05 ч), значит работает **ось возраста**. Это заранее оговорённый исход,
   переинтерпретации не подлежит.
   Keepalive впервые виден в поле (5491 строка; в 08-20 их было 0 — стояла на
   Trace): `slots=` даёт 9→583, 10→92, 11→22, 12→8 проходов, то есть 705 из 5491
   (12.8 %) охватили дренируемые слоты (>8 = `poolSize` достижимо только через
   `slotDraining`), а `slotConnecting`/`slotDead` кадров не получают. `spread_ms`
   — максимум, не сумма: mean 356 мс, **max 402** при cap 400, значения размазаны
   по 122…402 (кумулятивность `91a7ae2` не вернулась).
   ✅ **Причинная цепочка замкнута на САМИХ резавшихся слотах** — но на ДВУХ из
   трёх, и предъявлять надо именно так. У slot 2 и slot 3 последняя запись —
   keepalive, полученный УЖЕ В ДРЕНАЖЕ (через 2.2 и 0.8 с после входа), момент
   сходится с `last_write_age_ms` до долей секунды: 17:19:34.55 / факт .679 и
   18:16:36.69 / .904.
   ⚠ **Slot 8 в цепочку НЕ входит** (находка ревью): его keepalive пришёл в
   18:23:32.136, то есть за 2.2 с ДО входа в дренаж (18:23:34.326), в состоянии
   `slotReady` — такой кадр слот получил бы и без правки, поэтому его свежий
   `last_write_age_ms` = 4.8 с правке не атрибутируется; в дренаже он прожил
   всего 2.3 с. Первая версия записи гласила «у всех трёх» — направление верное,
   обоснование сильнее данных.
   ✅ **Риск «keepalive удержит дренаж» снят полем**: `drain_duration` не вырос —
   mean 0.47 с, упоров в sticky 0.9 % против 0.8 %. Канала влияния нет, как и
   предсказывало чтение кода.
   ⚠ **«Все 3 реза в дренаже» — ТАВТОЛОГИЯ, а не улика**, и это надо гасить
   сразу: **90.7 % всей экспозиции выше 80 с проходит в дренаже** (408.2 из
   450.2 слото-с; возраст входа в дренаж — mean 74.0 с по всем 3162 ротациям и
   78.7 с по подмножеству >80 с, при пороге 70 + stagger). Слот физически не
   доживает до 80 с, не войдя в дренаж, поэтому доля резов в нём информации почти
   не несёт — как и `cause=age_cut`.
   ⚠ **`drain_duration` печатается через `Truncate(time.Second)`**
   (`ws_pool_drain.go`), а `slot_age` — с миллисекундами, поэтому 92.1 % значений
   это `0s` при истинной величине в [0, 1). Усечение ЗАВЫШАЕТ расчётный возраст
   входа и потому НЕДОСЧИТЫВАЕТ время в дренаже: сдвиг +0.5 с даёт 95.8 %, +1.0 с
   — 98.5 %. То есть 90.7 % — **консервативная нижняя граница**, вывод от
   округления только усиливается. По той же причине Σ`drain_duration` = 1482 с и
   доля дренажа 0.64 % — тоже нижние границы (верх до ~2 %), не цитировать как
   точные.
   ⚠ **Снижение резов 5 → 3 улучшением НЕ является.** Нормировка на экспозицию:
   hazard 7.23e⁻³ 1/с (3 / 414.8 с) против 2.99e⁻³ (5 / 1671.7 с) — формально
   ВЫШЕ, но разница не значима (Пуассон p = 0.13; 95 % CI [1.5…21.1] против
   [1.0…7.0] перекрываются вчетверо). Упал ЗНАМЕНАТЕЛЬ: при идентичном пороге
   (`applied` = `configured` = 70 с, `adapt_changes=0` в обоих) хвост возрастов
   короче в 2.5 раза (плановых > 90 с: 2.24/ч против 5.76/ч). Верный вывод —
   **риск не изменился**.
   ⚠ Правило 9 воспроизвелось третий прогон подряд: стены нет — 20 плановых
   ротаций пережили самый старый рез (87.9 с) и сняты нами штатно, max 106.6 с.
   ✅ **Цена ИЗМЕРЕНА 2026-08-21** (была оценкой): дополнительные кадры =
   Σ max(0, `slots` − 8) = **865 за 8.05 ч ≈ 108/ч**, то есть **0.32 %** от
   271 148 `encrypts` — и это WS-фреймы в уже существующих соединениях, не новые
   TLS к origin. По кадрам в час это в 2.8 раза выше априорных 39/ч, по доле —
   ниже. Доля дренажа в слото-времени 0.64 % (1482 с / (28 970 × 8)).
   ⚠ Сравнивать `encrypts` между прогонами В ЛОБ нельзя: трафик вырос на 47 %
   (3.12 против 2.12 ГБ downlink, uplink в 6 раз), поэтому чистого A/B по цене
   из этих логов не получить — 0.32 % получено расчётом по числу проходов.
   ⚠ Цена на wire ограничена (лишний трафик к origin — P0-класс): дренаж =
   **0.79 %** слото-времени — 2053 с из 259 991 (= (10 ч 36 м − два зависания
   5695 с) × 8 слотов), средняя занятость **0.063 слота**; 90.7 % дренажей
   (3166 из 3490) закрываются с `drain_duration=0s`, то есть до keepalive не
   доживают. В кадрах: **+39/ч против базовых 5760 (+0.67 %)**, и это WS-фреймы
   в УЖЕ существующих соединениях, а не новые TLS к origin — то есть не тот
   класс, который является P0.
   ⚠ **Три ловушки в этом числе, все три я прошёл** (ревью 2026-08-21):
   (1) сумму `drain_duration` нельзя брать сырой — два дренажа по `36m51s`
   (2211 с) это артефакт сна, без их вычета получается 6475 с вместо 2053 с;
   (2) фильтр «< 3000 с» их НЕ ловит, потому что 2211 < 3000 — нужен вычет по
   смыслу, а не по порогу, и `awk`-парсер обязан разбирать `h`/`m`/`s`, иначе
   `2m4.038s` читается как 2 с;
   (3) знаменатель обязан быть однородным числителю: слото-секунды, а не секунды
   прогона. Делить 2053 с на время прогона даёт 5.38 % — это доля времени, когда
   дренирует ХОТЬ ОДИН слот, другая величина. Первая версия записи дала верные
   0.79 % из неверного знаменателя (9 ч × 8 при прогоне 10.6 ч) — то есть
   правильный ответ по неправильному обоснованию.
   ✅ `middleboxSilentCutFloor` = 10 с **объявлена в проде** с 2026-08-21
   (`client/ws_pool.go`, рядом с `keepaliveSpreadMax`, которую она ограничивает).
   До этого константа жила ТОЛЬКО в `ws_pool_keepalive_test.go`, из-за чего
   сторожа сравнивали продовую величину с тестовой: менять порог в проде было
   физически негде, а тесты продолжали бы проходить. Власть сторожей проверена
   порчей константы — при 4 с краснеют три теста, при возврате зелено.
   ⚠ Само число **наблюдаемое, но не доказанное как порог цензора**: в 08-20 три
   реза пришли при тишине 11.4/18.3/28.2 с (то есть выше 10 с), а в 08-21 тишина
   упала до 4.3–5.6 с и резы остались. Держать как консервативный инвариант
   разноса, не как измеренный порог.
   → Инструмент замера фазы **на проводе**, а не по логу: `tools/firstpackets
   -phase -pcap <файл> [-tick 0.5]` (SYN/FIN к origin, vector strength + слепой
   скан + CV интервалов). Захват — `pktmon`, нужны админские права.
   ⚠ Слепой скан возвращает **делитель** истинного периода, не сам период:
   сетка 500 мс лежит и на 250/125/100/50 мс с `R = 1.0` строго. Читать как
   «линия есть, период кратен найденному».
   ⚠ **Ловушка захвата на Wi-Fi, стоившая полного нуля на первом прогоне.**
   `pktmon` на беспроводном адаптере объявляет в IDB `LinkType 1` (Ethernet), а
   кадры пишет **802.11 + LLC/SNAP**. Парсер читал `[12:14]` как EtherType,
   получал мусор и отбрасывал всё: утилита сообщила «событий SYN/FIN к origin
   нет» на файле из 1 603 467 пакетов — потеря 100 %, молча. Исправлено
   (`tools/firstpackets/dot11.go`): смещение считается по типу кадра, а не
   фиксированное. В поле встретились два вида — FC `0x08` (Data, SNAP на 24) и
   `0x88` (QoS Data, SNAP на 26); ToDS+FromDS добавляет Address4 (+6). SNAP
   проверяется ПО ВЫЧИСЛЕННОМУ смещению, а не поиском `aa aa 03` по кадру: в
   payload эти байты встречаются случайно.
   → Признак этой ошибки, а не пустого захвата: файл большой (сотни МБ) и
   `pktmon etl2pcap` отчитался о миллионах пакетов, но `n=0` во всех сериях.
   ⚠ `worstCaseTeardown()` в проде = **142.5 с** (70 + 15.5 + 2 + 30 + 25), где
   слагаемое `sweep` = **`sweepWorstCase()` = 4 × tick** — ЕДИНСТВЕННЫЙ источник,
   не складывать по месту. Четыре тика, потому что пробуждений в худшем пути два
   (обнаружение + повтор после отсрочки гейта), а каждое длится до `tick + jitter`
   = 2 × tick. Формула ошибалась дважды за один день: `1×tick` (не учтено второе
   пробуждение) → `2×tick` (не учтён джиттер). В накладных `stagger` лишь 21 %, а
   `deferred 30 + tear 25` — 73 %; `drainRevertBackoff` в прод-прогоне не
   наблюдался ни разу. Сжимать бюджет через stagger/cap — неверная точка.
   ⚠ **`worstCaseTeardown()` НЕ является бюджетом полного пути в двух случаях, и
   оба важны, когда величину используют как ФИЛЬТР, а не как лог** (уточнено
   ревью 2026-08-17):
   (1) при `SHADOWLINK_GRACEFUL_DRAIN=0` из суммы выпадают `deferred` + `tear`
   (55 с в проде = 39 %), потому что legacy hard-rotation дренажа не имеет —
   остаётся «порог + stagger + свип» ≈ 84.5 с, что НИЖЕ наблюдаемых резов
   85–114 с. Поэтому верхняя граница правдоподобия в `slotobs` при выключенном
   дренаже отключается — иначе она отбраковала бы настоящие резы;
   (2) `drainCatastrophicBackoff` (150 с, `ws_pool_drain.go`, при
   `ready < poolSize/2`) в сумму не входит вовсе — слот может дожить до ~220 с,
   то есть ВЫШЕ границы. Это состояние деградации пула (hard rule 11), где резы
   как раз и наблюдались.
   → Следствие: величина берёт **адаптированный** `base`, а адаптер только
   сжимает порог (пол 30 с), поэтому граница может съехать до 102.5 с. Если
   использовать её как фильтр наблюдений — брать от `configured`, а не от
   применённого порога, иначе получается петля: сжатие порога сужает границу,
   та отбраковывает старшие наблюдения, что поддерживает сжатие.
   → Окно измерено 2026-08-14 **по экспозиции** (форма, не смещённая
   цензурированием): **0 резов на 52 808 с экспозиции ниже 80 с**, далее плато
   4–7e⁻³ 1/с до 105 с. Это не отвесная стена, а ступень.
   ⚠ Величина — **офлайн-реконструкция по логу**, клиент такую показать не может:
   при ринге плановых даже в 4096 записей экспозиция ниже 80 с ограничена его
   ёмкостью. Не искать 52 808 в `slot death hazard`. И «ожидалось бы ~190 резов»
   получено при hazard'е PROBE (~1.78 %/проход), а не прода (6.94 %) — при
   прод-цифре ожидалось бы ~760; знак вывода от этого только усиливается.
   Замер: `docs/plans/2026-08-13-age-wall-probe.md`.
10. **`SHADOWLINK_STAGGER_OFFSET_CAP` — no-op, пока `(2·poolSize − 1)·step ≤ cap`.**
   Кламп требует строгого `base > cap`; при `poolSize=8, step=1s` максимум
   `idx=15` даёт ровно `15 с`, поэтому при `cap=15s` он не срабатывает **ни на
   одной** ячейке. ⚠ Условие, а не свойство: `poolSize` конфигурируем
   (`WSPoolSize`), и при `poolSize ≥ 9` кламп оживает — иначе это была бы ровно
   та «глобальная константа, неверная в принципе», от которой предостерегает
   правило 8. При текущем проде правка 15 → 10 с связала бы только `idx 11…15`,
   чья занятость в поле — **0.16 %**, а после квантования тиком меняла бы корзину
   лишь у `idx=15` → **ноль соединений за прогон**. Причина заходов в полосу
   80–85 с — offsets 5…9 с у ячеек 5–9 плюс квантование, не cap. Вопрос закрыт
   2026-08-14 (независимая проверка); поднимать заново только с замером.

14. **`inflight_cap_deferred_total` — счётчик ХОЛОДНОГО СТАРТА, а не деградации**
   (разбор 2026-08-19). Величина выглядит как регресс при сравнении «в час»:
   прогон 104446 дал 27 за 2.31 ч (11.7/ч) против 11 за 7.99 ч (1.4/ч) в 094311,
   то есть рост в 8.5 раза, а по доле ротаций 2.89 % против 0.34 % — в 8.6 раза.
   **Оба сравнения ложные.** Разбивка по `uptime` показывает, что в ОБОИХ
   прогонах отложки набираются в первые ~3 минуты: 094311 — 11 к `3m2s`, дальше
   **0 за остальные 7 ч 56 м**; 104446 — 20 к `3m2s`, затем +2 за 2 ч 15 м и +5
   в последние 30 с при `active_streams` 98→70 (завершение процесса).
   Steady-state: **0.00/ч против 0.89/ч** — то есть разница в первых минутах, а
   не в устойчивом режиме, и объясняется профилем нагрузки (закачка с самого
   старта против постепенного разогрева).
   → Читать этот счётчик **только с разбивкой по `uptime`**, иначе он измеряет
   момент старта. Механизм безвреден по построению: гейт `inflight >
   maxConcurrentDrains` ставит `drainStormBrakeBackoff` (быстрый повтор), а не
   отменяет ротацию; `capacity_floor_deferred_total` при этом 0 в обоих прогонах.
   ⚠ Расхождение «счётчик 27, строк в логе 3» — **не потеря**, а намеренный
   rate-limit логирования (`shouldLogDeferred`, `ws_pool_drain.go:165`; вызов из
   гейта — `:659`). Счётчик инкрементится всегда, `total_count` в строке даёт
   реальный темп через дельту. Не искать здесь фантомный счётчик.

11. **Периодического healer'а ёмкости в пуле НЕТ** — каждая цепочка
   `reconnectLoop` одноразова, единственный вызов на старте это начальный connect.
   Поэтому обрыв цепочки = **потеря ячейки навсегда**. Так и было до 2026-08-14:
   `placeholder freed` → `reconnecting slot N` → дренаж забрал ту же ячейку →
   `reconnect short-circuited` → `empty` 8→9→10→11, `mean alive` 7.21→5.80 за 2 ч.
   На конце цепочки — пул на полу storm-brake, отклонённые ротации и резы
   посредника (3 из 4 в том прогоне). Исправлено: guard отказывается от ЯЧЕЙКИ, но
   восполняет ЁМКОСТЬ на свободной (`claimFreeCellForHealing`, предел
   `maxHealingRetargets`). ⚠ Лечение обязано быть УСЛОВНЫМ: лишнее TLS-соединение
   к origin — это P0 (policing по числу соединений), поэтому поднимать «на всякий
   случай» нельзя. Смотреть `healing_retargets_total` / `healing_gave_up_total`.
   ⚠ **`connectSlot` перезаписывает ячейку УСЛОВНО** (`cellIsOverwritable`,
   добавлено ревью 2026-08-14). `claimFreeCellForHealing` намеренно не ставит
   placeholder — иначе тот висел бы «здоровым» через весь backoff и глушил бы
   повторное лечение, а при `ctx.Done()` утёк бы вечным `slotConnecting`. Но из
   этого следует окно, в которое ячейку успевает занять `claimFreeSlot`, и
   первоначальная оценка «цена — лишнее одно соединение» **неверна**: перезапись
   живой ячейки осиротила бы TLS-соединение (все `Close()` идут через
   `slotAt(idx)`, то есть уже по новому указателю) и её `slotReader`
   (`shouldExitReader` сравнивает `generation` СВОЕГО объекта, а бампается
   `generation` нового), после чего этот ридер на своей ошибке чтения позвал бы
   `handleSlotDeath` **по индексу** и убил бы свежий слот. Защита — у писателя, а
   не в оценке вероятности: `slotReady`/`slotDraining` не перезаписываются,
   возвращается `ErrCellOccupied`; `nil`/`slotDead`/`slotConnecting`-placeholder —
   можно (на последнем держится штатная замена при дренаже). ⚠ `ErrCellOccupied`
   **не считать отказом origin**: ни лестница backoff, ни
   `reserveConnectFailures`, ни `AgeCutReconnectFail` к нему не относятся.
12. **Sticky — главный, но НЕ единственный источник хвоста возрастов** (замер
   2026-08-14, прогон 161915, 4 ч; перепроверено независимой реконструкцией
   n=1575). Перебег за 82 с — 254.0 с всего, из них sticky **196.8 с (77 %)**,
   а **natural finish 57.3 с (23 %)**. Соединений старше 85 с — 17, из них
   sticky 12, natural 5; max 102.6 с (sticky), 95.0 с (natural).
   ⚠ **«Почти единственный» — формулировка сильнее данных.** Доля sticky зависит
   от порога и растёт вниз: при 80 с это уже 66 % (222.8 из 339.7 с). Поэтому
   счётчик экспозиции накапливается на **общем пути** `tearDown`
   (`accrueAgeExposure`), а не в sticky-ветке — первая версия видела 77 % и
   недосчитывала четверть в безопасную сторону.
   ⚠ **`drain_duration` в ветке `age_backstop` — константа и о риске молчит**: все
   50 teardown'ов прогона дали ровно `25s`, потому что каждый упёрся в
   `stickyMaxDrainAge`, ни один не завершился раньше. Читать **`slot_age`** — он
   теперь есть во ВСЕХ ветках teardown, включая natural finish и hard cap.
   Механизм: дренаж по `byte_budget` (38 из 50) стартует в ЛЮБОМ возрасте —
   p50 34.2 с, p90 74.2 с, max 77.6 с, — и sticky добавляет ровно +25 с сверху,
   поэтому потолок жизни = `старт дренажа + 25 с` (наблюдаемый max 102.6 с
   сходится с 77.6 + 25).
   ⚠ **`slot_age` — возраст с момента ГОТОВНОСТИ соединения, не с SYN.**
   `startedAtNs` ставится в `connectSlot` после handshake, поэтому TCP+TLS
   handshake (в поле ~0.2–0.5 с) в величину не входит: реальная экспозиция на wire
   на столько же больше. Для сравнения с окном реза это в безопасную сторону.
   ⚠ **Но `slot_age_ms` в строке `WS pool slot reader error` — ДРУГАЯ величина:**
   она считается от локального `slotStart := time.Now()` в самом ридере
   (`client/ws_pool.go:4273`), а НЕ от `startedAtNs`. Для здоровых слотов они почти
   совпадают, поэтому расхождение незаметно — и именно на нулевых возрастах вся
   разница и есть сигнал. Уточнено 2026-08-17: из имени `slot_age*` родилась
   ложная гипотеза «`startedAtNs` не выставлен», тогда как настоящей причиной
   нулевых возрастов был отвергнутый сервером first-frame auth
   (`fakeAckAndClose` → нерасшифруемый ack → `UpgradeToWS` возвращал nil →
   слот вставал в `slotReady` пустым). Перед выводом о возрасте смотреть, ОТ ЧЕГО
   считает конкретное поле.
   → Вред пока НЕ наступил: 0 резов за 4 ч. Константу НЕ трогали (hard rule 8),
   вместо неё завели `age_exposure_over_threshold_ms`.
   ✅ Счётчик экспозиции считается на ВСЕХ путях терминации с 2026-08-17, и
   развязка от двойного счёта — **CAS `slot.ageExposureAccrued` на самом слоте, а
   не фильтр по `slotDeathCause`**. Причина отвечает «кто снял слот», а нужно
   «посчитано ли уже»: `deathCauseDrainTeardown` приходит в `handleSlotDeath` из
   ТРЁХ мест (`tearDown`, `tryForceEvictIdleSlot`,
   `tryEmergencyEvictMinStreamsSlot`), а учёт стоял в одном — фильтр по причине
   терял обе эвикции, то есть снятия старых слотов при нехватке ячеек, где перебег
   максимален. Сосед `recordPlannedRotation` стоит во всех трёх; сверять учёт с ним.
   ⚠ **Число ожидаемых резов из экспозиции НЕ выводится умножением на `rate_exp`.**
   Прежняя формулировка «при 49.2 с/ч и hazard 14.4e⁻³ 1/с ожидается ~1 рез»
   ошибочна дважды: 49.2 с/ч — это перебег за **85 с**, а не за 82 с (тот равен
   63.4 с/ч), и произведение даёт не 1, а **3–4** ожидаемых реза при
   наблюдённых 0 — то есть модель замером **отвергается**, а не подтверждает
   запас. Плюс `rate_exp` полосы 80–85 с неприменим к перебегу, набранному выше
   неё, и сам получен по 3 резам на 208 с. Это ровно та подмена единиц, которую
   разбирает `docs/plans/2026-08-14-post-fable-fixes.md` §2. Счётчик читать как
   **тренд**, не как прогноз.
   ⚠ Рост sticky 3.0/ч → 12.5/ч между прогонами объясняется нагрузкой (трафик
   1502 → 3116 МБ), но нормировка на объём снимает это лишь частично
   (`sticky/ГБ` 6.1 → 16.4) — чистый A/B требует одинакового профиля трафика.
   ✅ **Экспозиция считается на ОБОИХ путях с 2026-08-17.** До правки
   `accrueAgeExposure` имел единственный call-site — `tearDown` в дренаже, — а
   резы идут через `handleSlotDeath` мимо него: недосчёт **27 %** (дренаж 3010.1 с
   против фактических 4122.7 с; совпадение логируемых 3010102 мс с суммой только
   дренажных путей до миллисекунды и выдало границу). Добавлен
   `accrueAgeExposureOnDeath`, `deathCauseDrainTeardown` в нём исключён — иначе
   дренаж считался бы дважды. Сторож `TestAgeExposure_AccruedOnCutPath` проверяет
   оба знака.

13. **Полевой прогон — только с выключенным QuickEdit консоли** (свойства окна →
   снять «Выделение мышью») либо с выводом в файл, а не в консоль. Замер
   2026-08-17: случайное выделение мышью в PowerShell заблокировало вывод на
   **428 с**, и контур ротации встал целиком — 0 ротаций, возрасты соединений до
   **505.4 с** (в 3.6 раза выше `worst_case_teardown`), при том что данные шли на
   полной скорости (71 МБ за окно). Цензору причина остановки безразлична.
   ⚠ **Как отличить это от блокировки в коде за одну команду** — не искать
   виновника внутри процесса, а проверить ФАЗУ и число дропнутых тиков:
   при внешней приостановке вывода фаза тикеров сохраняется (`stats` шёл на
   `.590`, выдал одну внеплановую строку на `.258` и вернулся на `.590`), а тики
   дропаются, не копятся (6847 против 6932 ожидаемых = 85 × 5 с ≈ длина окна).
   Голодавший или разбуженный после suspend тикер дал бы сдвинутую фазу, а
   заблокированный `slog`-обработчик — очередь строк с ИСХОДНЫМИ метками.
   Дополнительный признак: файл лога перестаёт быть хронологичным (340 инверсий,
   264 строки с одной меткой — флеш очереди), поэтому **awk-диапазоны по такому
   логу считают не то**; окна выбирать фильтром по метке времени.
   ⚠ Все восемь держателей `reserveMu` в этом прогоне были прочитаны и
   признаны чистыми — сетевых операций под локом нет. Не искать там регресс
   заново без нового замера.

## Architecture

```
App → SOCKS5 → Client → uTLS (Chrome 133) → direct origin IP :443
    → nginx (TLS term) → 127.0.0.1:10443 → Server → Internet
```
Routing (`server/handler.go`): `POST`+`application/json` → VPN-сессия ·
`Upgrade: websocket` → WS transport · всё прочее → decoy SPA.

Crypto: X25519 ECDH → HKDF-SHA256 → AES-256-GCM, forward secrecy, replay
protection. Стеганография на HTTP-уровне, не TLS-уровне.

## Packages

| Package | Purpose |
|---|---|
| `core/` | Crypto, session manager, слоты/миграция, flow control, jitter |
| `server/` | HTTP handler, tunnels, mux, decoy, rate limiter, auth, metrics, management |
| `client/` | DirectTransport, ConnManager, leakguard, dnsproxy (split-DNS), bypassroute |
| `proxy/` | SOCKS5 (+ UDP associate) |
| `skins/browser/` | Mimicry engine — inflation, shaping, cover, fingerprint registry |
| `cmd/shadowlink-server/` | Server entry + `export-client-config` |
| `cmd/nixavpn-client/` | Unified client CLI (ShadowLink + VLESS), tun2socks, tunnel.go |
| `testutil/`, `tools/` | Хелперы · утилиты |

## Dev commands

```bash
go test ./... -count=1          # Windows: без -race
go build ./... && go vet ./...
bash build-server.sh           # → bin/shadowlink-server-linux
bash build-client.sh           # → $CLIENT_BIN_DIR (default ./bin = D:\shadowlink\bin)
```

## Triggers → load skills

| Задача | Skill |
|---|---|
| FP/uTLS, decoy, cover, wire-формат, «а если через CDN» | `mimicry-model` |
| Ротация слотов, drain, тайминги, `SHADOWLINK_*` флаги, cold start | `anti-tspu-tuning` |
| Сборка, деплой, nginx, systemd, server CLI, connection failures | `deployment` |
| Написание/отладка тестов, `-race`, fuzz, флейки под `-shuffle` | `testing-rules` |

## Isolation rules

- Весь ShadowLink-код — в этом репозитории. Docs/specs — `docs/`. Тесты
  colocated (`*_test.go`).
- В дерево NixaVPN идут только integration points (deploy orchestrator, config
  assembler, admin handlers). Путь резолвится через `internal/slpath` / env
  **`SHADOWLINK_DIR`** — хардкода пути быть не должно.

## Key docs

- `docs/PHASES-CHANGELOG.md` — летопись фаз/аудитов; читать при откате фичи
- `docs/protocols/` — `body-prefix-v1`, `flow-control-v2`, `live-decoy`
- `docs/strategy/2026-05-03-final-audit/MASTER.md` — последний полный аудит

## Planned work

1. **Embedded tun2socks** — сейчас внешний бинарь, встроить как Go-пакет.
2. **NixaVPN integration** — deploy orchestrator, config assembler, admin handlers.
3. **18-й раунд аудита** — многотрековый, с web-research (запрошен 2026-07-25).
