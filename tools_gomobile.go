//go:build tools

// Файл существует только ради go.mod: пакет mobile/ собирается в .aar внешним
// инструментом (gomobile), а тот требует, чтобы golang.org/x/mobile была в
// зависимостях модуля. Ни один рабочий файл её не импортирует, поэтому без
// этой записи `go mod tidy` вычистил бы её — и сборка .aar сломалась бы позже
// и у другого человека, с сообщением про отсутствующий пакет, а не про tidy.
//
// Build-тег гарантирует, что в обычную сборку файл не попадает.
package tools

import _ "golang.org/x/mobile/bind"
